<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Games</title>
</head>
<body>
  <h1>All Games Passed</h1>

  <% if (games.length > 0) { %>
    <% games.forEach(game => { %>
      <h3><%= game.title %></h3>
      <img alt="<%= game.title %> Game Cover" src="<%= game.image %>"/>
      <p><%= game.genre %></p>
      <p><%= game.console %></p>
      <p><%= game.year %></p>
      <p><%= game.description %></p>
      <p><%= game.datePassed %></p>
    <% }) %>
  <% } else { %>
    <h2>There are no games to display...</h2>
  <% } %>
  
  
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/about">About</a></li>
    <li><a href="/api/games">Add Game</a></li>
    <li><a href="/api/ratings">Rating System</a></li>
  </ul>
  <p></p>
</body>
</html>